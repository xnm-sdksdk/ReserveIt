quarkus.http.port=3003

quarkus.package.remove-duplicate-files=true
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${AUDIT_USER}
quarkus.datasource.password=${AUDIT_PWD}
quarkus.datasource.jdbc.url=jdbc:postgresql://postgres-resource:5432/${AUDIT_DB}

quarkus.hibernate-orm.database.generation=update
quarkus.package.jar.type=fast-jar

quarkus.liquibase.enabled=false
quarkus.container-image.build=false

mp.jwt.verify.publickey.location=/deployments/jwt_public.pem
mp.jwt.verify.issuer=user-service
quarkus.smallrye-jwt.enabled=true
